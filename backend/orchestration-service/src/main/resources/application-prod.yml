# Production profile: set CORS_ALLOWED_ORIGINS (comma-separated) in env
app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://your-frontend-domain.com}

logging:
  level:
    root: INFO
    com.foodai.orchestration: INFO
